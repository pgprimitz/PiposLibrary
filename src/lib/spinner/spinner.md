# generic-spinner

Indicador de carga. El label es solo accesible.

## Inputs

| Nombre | Tipo | Default |
| --- | --- | --- |
| `size` | `sm \| md \| lg` | `md` |
| `label` | `string` | `Cargando` |

## Uso

```html
<generic-spinner size="lg" label="Guardando cambios" />
```
